import 'package:flutter/material.dart';

class CartsView extends StatelessWidget {
  const CartsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("carts view"),
    );
  }
}
